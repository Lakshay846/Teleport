export interface Flight {
    id: string;
    aoc: string;
    flightNumber: string;
    origin: string;
    destination: string;
    std: string;
    sta: string;
    daysOfOperation: number[];
    bodyType: string;
    startDate: string;
    endDate: string;
    status: string;
  }